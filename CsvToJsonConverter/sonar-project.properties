# must be unique in a given SonarQube instance
sonar.projectKey=csv-to-json-converter
# this is the name displayed in the SonarQube UI
sonar.projectName=CSV to JSON Converter
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=./CsvToJsonConverter

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Look for the OpenCover coverage report in the same directory.
sonar.cs.opencover.reportsPaths=opencover.xml
